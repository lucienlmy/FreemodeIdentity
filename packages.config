<?xml version="1.0" encoding="utf-8"?>
<!-- No NuGet runtime dependencies: appearance persistence uses the framework's
     System.Xml.Serialization, and SHVDN/LemonUI are referenced from
     ..\packages\ (the player's install), not restored by NuGet. -->
<packages>
</packages>
